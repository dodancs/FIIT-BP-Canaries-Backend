{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Launch app",
            "type": "shell",
            "command": "ubuntu",
            "args": [
                "run",
                "php",
                "-S",
                "localhost:8000",
                "-t",
                "public"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}